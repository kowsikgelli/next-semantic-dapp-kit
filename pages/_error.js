import Layout from '../components/Layout';

export default ()=>{
    return(
        <Layout>
            <h2>Error occured while fetching data in index.js getInitialProps function</h2>
        </Layout>
    )
}